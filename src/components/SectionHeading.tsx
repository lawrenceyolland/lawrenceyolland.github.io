export const SectionHeading = ({ text }: {text: string}) => (
    <h3 className="text-xl font-semibold uppercase tracking-widest text-gray-900 dark:text-slate-500 whitespace-pre">
        {text}
    </h3>
)